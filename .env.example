# OpenAI
OPENAI_API_KEY=sk-...

# Twilio
TWILIO_ACCOUNT_SID=AC...
TWILIO_AUTH_TOKEN=...
TWILIO_WHATSAPP_NUMBER=whatsapp:+14155238886

# Google Sheets (must be shared as "Anyone with the link" → Viewer)
GOOGLE_SHEETS_ID=your-spreadsheet-id-here
GOOGLE_SHEETS_GID=0

# Message buffering — wait this many seconds after the last message before processing
MESSAGE_BUFFER_SECONDS=30

# PostgreSQL (provided automatically by Railway Postgres add-on)
DATABASE_URL=postgresql://user:pass@host:5432/dbname

# Daily message limit per user (unset = unlimited)
DAILY_MESSAGE_LIMIT=50

# Secret token for admin endpoints (e.g. resetting a user's message counter)
ADMIN_TOKEN=your-secret-token-here
